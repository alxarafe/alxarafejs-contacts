meta {
  name: Add channel (email by id)
  type: http
  seq: 80
}

post {
  url: {{BASE_URL}}/contacts/{{contactId}}/channels
  body: json
  auth: none
}

body:json {
  {
    "channelTypeId": {{emailChannelTypeId}},
    "value": "ada@work.example.com",
    "label": "trabajo"
  }
}